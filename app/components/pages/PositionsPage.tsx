'use client';

import PageWrapper from '../common/PageWrapper';

export default function PositionsPage() {
  return (
    <PageWrapper title="Positions">
      <div className="text-[var(--color-text-muted)]">
        View your active positions on DeepBook Predict
      </div>
    </PageWrapper>
  );
}