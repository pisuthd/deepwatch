'use client';

export default function RecentInsightsPage() {
  return (
    <div className="flex items-center justify-center h-full text-[var(--color-text-muted)]">
      <div className="text-center">
        <h2 className="text-xl font-semibold text-[var(--color-text-primary)] mb-2">Recent Insights</h2>
        <p>View recent insights shared by the community</p>
      </div>
    </div>
  );
}