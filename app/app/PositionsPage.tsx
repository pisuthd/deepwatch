'use client';

export default function PositionsPage() {
  return (
    <div className="flex items-center justify-center h-full text-[var(--color-text-muted)]">
      <div className="text-center">
        <h2 className="text-xl font-semibold text-[var(--color-text-primary)] mb-2">Positions</h2>
        <p>View your active positions on DeepBook Predict</p>
      </div>
    </div>
  );
}